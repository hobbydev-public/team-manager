package hobbydev.teammanager.business.validations;

import hobbydev.teammanager.domain.accounts.User;
import hobbydev.teammanager.domain.projects.Project;

public class UserToProjectAccessValidations {
	
	public static boolean canUserEditProject(User user, Project project) {
		boolean can = true;
		
		return can;
	}
	
	public static boolean canUserDeleteProject(User user, Project project) {
		boolean can = true;
		
		return can;
	}
}
